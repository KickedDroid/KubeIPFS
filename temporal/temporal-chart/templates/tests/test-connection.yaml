apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "temporal-chart.fullname" . }}-test-connection"
  labels:
{{ include "temporal-chart.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "temporal-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
